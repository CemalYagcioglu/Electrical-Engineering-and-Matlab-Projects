%time = input('What is the time?\n');
%initial_charge = input('What is the initial charge?\n');
%resistance = input('What is the resistance?\n');
%inductance = input('What is the inductance?\n');
%capacitance = input('What is the capacitance?\n');
%varlist2(time,initial_charge,resistance,inductance,capacitance);



function varlist2(time,inital_charge,resistance,inductance,capacitance)
   fprintf('Total number of inputs = %d\n',nargin);
   
   
end
%function varlist(time,initial_charge,resistance,inductance,capacitance)
%lenghty=length(varlist);
%disp(lenghty)
%end
%x=0;
%y=0;
%c = input(x,y,varargin);
%function Charge(X,Y,varargin)
%d =input('time','initial_charge','resistance','inductance','capacitance');
%fprintf('Total number of inputs = %d\n',nargin);
%end





%Charge = @(time,initial_charge,resistance,inductance,capacitance) 